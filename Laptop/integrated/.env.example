# Copy these exports into your shell profile or run them before eeg_reader.py.
# Never commit real API credentials.
export APP_KEY="your_app_key"
export APP_SECRET="your_app_secret"
export CLIENT_ID="focusflow_user_001"

# Optional: fix the headband address and dashboard WebSocket binding.
# export FLOWTIME_MAC="AA:BB:CC:DD:EE:FF"
export WS_HOST="0.0.0.0"
export WS_PORT="8765"
